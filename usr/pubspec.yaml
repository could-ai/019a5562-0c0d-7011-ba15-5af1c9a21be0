name: couldai_user_app
description: "Face Launcher - Um launcher inteligente com interface de rosto 2D controlado por voz"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # TODO: Adicionar quando implementar funcionalidades reais:
  # speech_to_text: ^7.0.0  # Reconhecimento de voz
  # permission_handler: ^11.3.1  # Gerenciar permissões
  # flutter_local_notifications: ^18.0.1  # Notificações e lembretes
  # url_launcher: ^6.3.1  # Abrir apps via URL schemes
  # android_intent_plus: ^5.1.0  # Intents Android para abrir apps
  # shared_preferences: ^2.3.3  # Armazenar preferências
  # intl: ^0.19.0  # Formatação de data/hora para lembretes

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/sounds/  # Sons de feedback futuro
  #   - assets/animations/  # Animações adicionais futuro
